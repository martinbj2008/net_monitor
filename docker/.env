POSTGRES_DB=probe
POSTGRES_USER=probe
POSTGRES_PASSWORD=jPPErRzPbz0cMo2r8TfM1MD4

GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=bOSoqzmlbrO0vPeeV7au3it9

# Postgres bind address. Since wg retirement all probing runs on the HUB itself
# (hub-mode: daemon writes directly to 127.0.0.1); no cross-host DB writes.
PG_BIND=127.0.0.1:25432
